# configs/dataset/celeba.yaml
_target_: faithful_cond_gen.data.celeba.CelebaDataConfig

cache_dir: /mnt/pvc/AutoSync/data/celeba_cache
image_size: [224, 224]    # Matches model 64 for speed
augment_train: true
normalize: true
batch_size: 16          # Per GPU
num_workers: 4
rare_threshold: 50
selected_attrs: null
held_out_combos: null