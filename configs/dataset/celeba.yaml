# conf/dataset/celeba.yaml

_target_: faithful_cond_gen.data.celeba.CelebaDataConfig

cache_dir: /mnt/pvc/AutoSync/data/celeba_cache
image_size: [224, 224]
augment_train: false
normalize: false
batch_size: 64
num_workers: 4
rare_threshold: 50

# attributes to condition on (you can override in experiment)
selected_attrs: null    # defaults to COMPOSITION_ATTRS_DEFAULT
held_out_combos: null