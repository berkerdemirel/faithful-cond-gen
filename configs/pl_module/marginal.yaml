# configs/pl_module/marginal.yaml
# DEPRECATED: Use variant=compositional instead
# Kept for backward compatibility only
#
# Example: python run_experiment.py --config-name=config_celeba_marginal variant=compositional
_target_: faithful_cond_gen.pl_modules.generator_pl.GeneratorPLConfig

lr: 1e-4
weight_decay: 0.0
warmup_steps: 1000

cond_dropout_prob: 0.2
additivity_loss_weight: 1.0
additivity_loss_prob: 0.25
additivity_t_threshold: 0.7
attr_delta_loss_weight: 0.0
relational_loss_weight: 0.0

val_target_conditions: null
