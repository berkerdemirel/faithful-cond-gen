# configs/config_rxrx1.yaml
defaults:
  - dataset: rxrx1        # celeba, rxrx1
  - model: generator_rxrx1 # generator_celeba, generator_rxrx1
  - pl_module: default     # GeneratorPL settings (lr, etc)
  - train: default_rxrx1           # Trainer settings (50 batches)
  - eval: rfid
  - logger: wandb_rxrx1          # WandB Logger
  - callbacks: checkpoint # Checkpoint Callback
  - _self_

seed: 1337
device: cuda