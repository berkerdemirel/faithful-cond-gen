# configs/config.yaml
defaults:
  - dataset: celeba        # Changed from rxrx1
  - model: generator
  - pl_module: default     # GeneratorPL settings (lr, etc)
  - train: debug           # Trainer settings (50 batches)
  - eval: rfid
  - logger: wandb          # WandB Logger
  - _self_

seed: 1337
device: cuda