# @package _global_
# configs/variant/vanilla.yaml
# Vanilla baseline: all regularizers OFF

model:
  use_repa: false
  repa_proj_coeff: 0.0

pl_module:
  cond_dropout_prob: 0.0
  additivity_loss_weight: 0.0
  attr_delta_loss_weight: 0.0
  relational_loss_weight: 0.0
